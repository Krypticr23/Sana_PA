# Minimum floors (not hard pins) so pip pulls current releases that ship
# prebuilt wheels for new Python versions (e.g. 3.14) — avoids compiling
# pydantic-core from Rust source.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.11.0
python-multipart>=0.0.12

# Apple Calendar (EventKit) — Mac only. The platform marker means this is
# skipped automatically on the Jetson/Linux, so `pip install -r requirements.txt`
# works on both nodes.
pyobjc-framework-EventKit>=10.0; sys_platform == "darwin"
